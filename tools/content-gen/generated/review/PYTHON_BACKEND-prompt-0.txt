당신은 개발자 진단 문항을 검수한다. 트랙: PYTHON_BACKEND

각 문항에 대해 아래 네 가지를 본다.
1. 사실오류 — 기술적으로 틀린 서술
2. 정답키 오류 — answerKey.correct 가 가리키는 보기가 정답이 아님
3. 정답을 흘리는 선택지 — 길이·구체성만으로 정답이 보이는 문항
4. 어색한 한국어 — 번역투·비문

문제가 없는 문항은 결과에 넣지 않는다. 결과는 JSON 배열만 출력한다(설명 금지): [{"index": 0, "axis": "정답키", "detail": "...", "suggestion": "..."}]

{"index": 0, "content": "Django에서 ORM 쿼리셋 지연 평가를 비활성화하려면 어떻게 해야 하나?", "options": [list() 호출하기, filter() 사용하기, get() 메서드 사용하기, all() 메서드 사용하기], "correct": 0}
{"index": 1, "content": "비동기 함수에서 `await asyncio.sleep(1)` 호출이 실행될 때 이벤트 루프는 어떻게 동작하는가?", "options": [루프가 오류를 발생시킨다., 루프가 잠시 중지되어 다른 작업을 처리할 수 있다., 루프가 완전히 멈추어 모든 비동기 작업이 중단된다., 루프가 계속 실행되지만 `asyncio.sleep`은 무시된다.], "correct": 1}
{"index": 2, "content": "Django에서 트랜잭션을 시작하고 특정 로직이 완료되면 자동으로 커밋되도록 설정하려면 어떻게 해야 하나?", "options": [session.commit() 호출하기, db.session.commit() 호출하기, @transaction.atomic() 데코레이터 사용, @atomic 데코레이터 사용], "correct": 2}
{"index": 3, "content": "FastAPI에서 백그라운드 태스크를 실행하려면 어떻게 해야 하는가?", "options": [@background 데코레이터 적용, threading.Thread 사용하기, multiprocessing.Process 사용하기, background_tasks.add_task() 호출하기], "correct": 3}
{"index": 4, "content": "GIL(Global Interpreter Lock)의 주요 기능은 무엇인가?", "options": [파이썬 인터프리터가 한 번에 하나의 스레드만 실행하게 만든다., 파이썬 코드에서 동기 호출을 비동기로 변환한다., 파이썬 코드를 컴파일하여 빠른 실행을 가능하게 한다., 파이썬 프로그램에서 동시에 여러 스레드를 실행할 수 있게 한다.], "correct": 0}
{"index": 5, "content": "파이썬 컨텍스트 매니저(`with` 문)는 어떤 기능을 제공하는가?", "options": [파일을 읽고 쓰는 동안 오류를 무시한다., 특정 스코프에서 리소스를 안전하게 정리하거나 초기화할 수 있게 한다., 동기 코드를 비동기 호출로 바꾼다., 함수의 결과를 캐싱하여 성능을 향상시킨다.], "correct": 1}
{"index": 6, "content": "WSGI와 ASGI의 주요 차이점은 무엇인가?", "options": [WSGI는 비동기 호출만 지원한다., ASGI는 동기 호출만 지원한다., WSGI는 HTTP 요청 응답 프로토콜을 사용하며, ASGI는 HTTP 및 WebSocket 연결을 동시에 처리할 수 있다., ASGI는 HTTP 요청 응답 프로토콜만 사용한다.], "correct": 2}
{"index": 7, "content": "파이썬 제너레이터(`yield`)의 주요 특징은 무엇인가?", "options": [함수 내부의 로컬 변수 상태를 유지하지 않는다., 함수 호출 시 함수 본체 코드가 즉시 실행된다., 함수에서 많은 양의 데이터를 한 번에 반환한다., 함수가 여러 값을 순차적으로 반환할 수 있도록 한다.], "correct": 3}
{"index": 8, "content": "중첩된 리스트를 포함한 객체를 얕은 복사(shallow copy)했을 때 나타나는 현상으로 옳은 것은?", "options": [최상위 객체는 새로 생성되지만 내부의 중첩 객체는 원본과 참조를 공유한다., 모든 계층의 객체가 완전히 새로 생성되어 원본과 전혀 무관해진다., 복사가 이루어지지 않고 원본 객체의 참조만 반환된다., 얕은 복사는 깊은 복사보다 항상 느리게 동작한다.], "correct": 0}
{"index": 9, "content": "파이썬 데코레이터가 함수를 감싸는 방식은 어떻게 작동하는가?", "options": [데코레이터는 단순히 함수의 결과를 반환한다., 데코레이터는 함수 호출 전후로 코드를 추가할 수 있다., 데코레이터는 함수 내부에서 동작을 변경하지 않는다., 데코레이터는 함수 자체를 수정하거나 대체하는 역할만 한다.], "correct": 1}
{"index": 10, "content": "스레드(thread)와 코루틴(coroutine)의 동시성 처리 차이점은 무엇인가?", "options": [스레드는 비동기 작업만 지원한다., 코루틴은 동시에 여러 작업을 실행할 수 없다., 스레드는 CPU 스케줄링에 의존하여 실행되지만, 코루틴은 프로그래머가 직접 제어를 넘겨받는다., 코루틴은 항상 메모리 사용량이 적다.], "correct": 2}
{"index": 11, "content": "`asyncio.gather` 함수의 주요 기능은 무엇인가?", "options": [비동기 작업들을 순서대로 호출하여 실행한다., 비동기 작업들의 결과를 무시한다., 단일 비동기 작업을 동기적으로 실행한다., 여러 비동기 작업 결과를 동시에 처리하고 반환한다.], "correct": 3}
{"index": 12, "content": "파이썬 예외 계층에서 커스텀 예외를 정의할 때 어떤 규칙을 따라야 하는가?", "options": [커스텀 예외는 적절한 부모 클래스를 상속 받아야 한다., 예외 클래스 이름은 반드시 `Exception` 클래스로 시작해야 한다., 커스텀 예외는 항상 터미널 오류(Exception)을 상속받아야 한다., 커스텀 예외는 `BaseException` 클래스를 직접 상속해야 한다.], "correct": 0}
{"index": 13, "content": "함수가 정수 id를 받아 User 객체 또는 None을 반환할 수 있을 때, 이를 타입 힌트로 정확히 표현한 것은?", "options": [def get_user(id: int) -> User: ..., def get_user(id: int) -> Optional[User]: ..., def get_user(id) -> int: ..., def get_user(id: int) -> None: ...], "correct": 1}
{"index": 14, "content": "가변 기본 인자(mutable default argument) 문제를 피하는 방법은 무엇인가?", "options": [모든 가변 객체는 불변 객체로 바꾸어야 한다., 가변 객체를 항상 함수 외부에서 전달해야 한다., 매개변수의 기본 값으로 `None`을 사용하고, 함수 내에서 필요할 때 값을 초기화한다., 함수 매개변수에 무결성 검사를 추가한다.], "correct": 2}
{"index": 15, "content": "블로킹 호출(예: 동기 I/O)이 이벤트 루프를 멈추는 문제를 피하는 방법은 무엇인가?", "options": [블로킹 호출을 무시하고 계속 실행한다., 동기 함수를 비동기 함수로 변경한다., 비동기 함수를 동기 함수로 변경한다., 비동기 I/O를 사용하여 블로킹 호출을 피한다.], "correct": 3}
{"index": 16, "content": "Django ORM 쿼리셋은 언제 데이터베이스에 실제 쿼리를 실행하나?", "options": [쿼리셋을 순회하거나 리스트로 변환할 때, 위 모두, 데이터 조회 시 바로 실행, 필터링이나 정렬을 적용할 때], "correct": 0}
{"index": 17, "content": "Django에서 OneToOneField 또는 ForeignKey 관계의 N+1 문제를 해결하기 위해 select_related를 사용하는 방법은?", "options": [queryset.filter(field_name__in=values), queryset.select_related('field_name'), queryset.prefetch_related('related_field'), model.objects.annotate(field_name='value')], "correct": 1}
{"index": 18, "content": "Django 마이그레이션을 생성하고 적용하기 위한 순서는?", "options": [makemigrations -> runserver, syncdb -> migrate, makemigrations -> migrate, migrate -> makemigrations], "correct": 2}
{"index": 19, "content": "Django 미들웨어가 요청/응답 처리 흐름에서 어떤 순서로 실행되는지?", "options": [등록된 모든 미들웨어가 동시에 실행, 미들웨어 등록 순서대로 반복 실행, 등록 순서와 관계 없이 무작위로 실행, 첫 번째 미들웨어부터 마지막 미들웨어까지 순차적으로 실행], "correct": 3}
{"index": 20, "content": "Django 프로젝트에서 개발(dev)과 운영(prod) 환경의 설정값을 분리해 관리하는 가장 흔한 방법은?", "options": [settings/base.py에 공통 설정을 두고, settings/dev.py·settings/prod.py가 이를 상속한 뒤 DJANGO_SETTINGS_MODULE 환경변수로 선택한다., 환경에 관계없이 하나의 settings.py만 두고 조건 없이 사용한다., 운영 배포 시마다 settings.py 파일 내용을 수동으로 덮어쓴다., settings.py를 데이터베이스에 저장해 런타임에 조회한다.], "correct": 0}
{"index": 21, "content": "@transaction.atomic() 블록 안에서 예외가 발생했을 때의 동작은?", "options": [변경 사항 유지 또는 롤백은 설정에 따라 달라짐, 예외 발생 시 모든 변경사항 롤백, 예외 발생 시 변경 사항 유지, 예외 발생 여부와 관계없이 항상 변경 사항 저장], "correct": 1}
{"index": 22, "content": "Django REST Framework Serializer의 주요 역할은 무엇인가?", "options": [뷰(View) 로직 처리, 쿼리셋 필터링, 데이터 직렬화와 검증, 데이터베이스 쿼리 생성], "correct": 2}
{"index": 23, "content": "DRF ModelSerializer로 모델 필드를 자동 매핑하는 방법은?", "options": [serializers.Serializer에서 모든 필드 수동 지정, serializers.ListSerializer 사용, views.ModelViewSet으로 ViewSet 정의, serializers.ModelSerializer에서 모델 클래스 정의], "correct": 3}
{"index": 24, "content": "DRF ViewSet과 라우터(Router)가 URL을 자동 생성하는 방식은?", "options": [라우터에 등록된 모든 ViewSet에 대해 라우트 자동 생성, 라우터 등록 시 URL 패턴 지정, ViewSet에서 urls.py로 직접 등록, 자동으로 생성되므로 설정 필요 없음], "correct": 0}
{"index": 25, "content": "DRF 권한 클래스(permission class)로 접근을 제어하는 방법은?", "options": [view 함수에서 직접 체크, permission_classes 속성을 통해 적용, urls.py에서 설정, settings.py에 전역 권한 목록만 정의], "correct": 1}
{"index": 26, "content": "DRF에서 커서 기반 페이지네이션(cursor-based pagination)과 오프셋 기반 페이지네이션(offset-based pagination)의 주요 차이는?", "options": [커서는 무작위 접근 가능, 오프셋은 순차적 접근만 가능, 오프셋은 무작위 접근 가능, 커서는 순차적 접근만 가능, 커서는 특정 객체 ID를 사용, 오프셋은 객체 수를 사용, 오프셋은 특정 객체 ID를 사용, 커서는 객체 수를 사용], "correct": 2}
{"index": 27, "content": "DRF 중첩 Serializer(nested serializer)가 N+1 쿼리를 유발하는 상황은?", "options": [모든 모델 필드를 단순 직렬화할 때, 모델 간 관계가 없는 경우, 단일 모델 필드만 직렬화할 때, 하나의 모델이 다른 여러 모델과 연관되어 있고, 목록을 직렬화할 때마다 각 항목의 연관 객체를 별도로 조회할 때], "correct": 3}
{"index": 28, "content": "Django 시그널(signal)이 이벤트 발생 시 리시버를 호출하는 방식은?", "options": [특정 이벤트에 대해 시그널 등록 후, 해당 이벤트 발생 시 자동 호출, 리시버 함수 직접 호출, 모든 이벤트에 대한 시그널 등록 후, 단일 리시버로 모든 이벤트 처리, 뷰(View)에서 수동으로 시그널 전송], "correct": 0}
{"index": 29, "content": "Django에서 시그널 대신 명시적 함수 호출을 선택해야 하는 상황은?", "options": [모든 시나리오에서 항상 사용, 특정 이벤트에 대한 복잡한 처리나 실행 순서 보장이 필요할 때, 뷰(View)에서 데이터 조회만 필요할 때, API 엔드포인트로 데이터 요청할 때], "correct": 1}
{"index": 30, "content": "FastAPI에서 경로 매개변수(path parameter)를 선언하는 방법은 무엇인가요?", "options": [경로 매개변수는 반드시 Query 클래스로 감싸서 지정해야 한다., path parameter는 @path_parameter 데코레이터를 사용하여 지정한다., 경로 템플릿의 {변수명}과 이름이 같은 함수 매개변수를 선언하고 타입 힌트를 붙이면 FastAPI가 자동으로 경로 매개변수로 인식한다., 경로 매개변수는 path parameter 키워드를 이용해 설정할 수 있다.], "correct": 2}
{"index": 31, "content": "FastAPI Pydantic 모델이 요청 바디를 자동으로 검증하는 방식은 무엇인가요?", "options": [Pydantic 클래스 변수의 타입을 직접 정의한 뒤, 이를 함수 매개변수로 사용해 검증한다., Pydantic 모델을 직접 사용하여 요청 바디를 검증한다., request_body 데코레이터로 요청 바디의 타입 힌트를 지정하면, FastAPI는 Pydantic 모델을 자동으로 생성하고 검증한다., 함수 매개변수에 Pydantic 모델 타입 힌트를 사용하여 지정하면, FastAPI는 자동으로 요청 바디를 검증한다.], "correct": 3}
{"index": 32, "content": "FastAPI `Depends()`로 의존성 주입을 구성하는 방법은 무엇인가요?", "options": [매개변수의 기본값으로 Depends(function)을 지정하면 FastAPI가 이를 해석해 자동으로 의존성을 주입한다., `depends()` 함수는 의존성을 반환하고, 이를 매개변수로 사용하여 의존성을 주입한다., 함수 내에서 직접 의존성 객체를 생성하고, 이 객체를 다른 메서드에 전달하여 주입한다., `@depends()` 데코레이터를 사용해 의존성을 선언하고, 이를 함수 매개변수로 활용하면 의존성이 주입된다.], "correct": 0}
{"index": 33, "content": "FastAPI `response_model`로 응답 스키마를 제한하는 방법은 무엇인가요?", "options": [함수 매개변수에 Pydantic 모델 타입 힌트를 사용하여 지정하면, FastAPI는 자동으로 응답 스키마를 제한한다., 경로 데코레이터(`@app.get(..., response_model=Model)`)에 Pydantic 모델을 지정하면 FastAPI가 응답을 그 스키마에 맞춰 직렬화·검증한다., Pydantic 모델 클래스 변수에 직접 타입을 정의한 뒤, 이를 함수 매개변수로 사용하여 응답 스키마를 제한한다., 함수 내에서 직접 Pydantic 모델을 반환하고, 이를 `response_model`으로 설정한다.], "correct": 1}
{"index": 34, "content": "FastAPI가 Pydantic 모델로부터 OpenAPI 스키마를 자동 생성하는 원리는 무엇인가요?", "options": [FastAPI는 직접 Pydantic 모델의 메서드를 호출하여 데이터베이스에서 스키마 정보를 가져와 자동으로 생성한다., Pydantic 모델과 무관하게 함수의 docstring만 분석하여 OpenAPI 스키마를 생성한다., 함수 매개변수와 반환값(response_model)에 사용된 Pydantic 모델의 필드·타입을 분석해 OpenAPI 스키마를 생성한다., 함수 매개변수와 반환값에 Pydantic 모델을 사용하면, FastAPI는 이를 바탕으로 OpenAPI 스키마를 생성한다.], "correct": 2}
{"index": 35, "content": "HTTP 상태 코드 401과 403의 의미 차이는 무엇인가요?", "options": [401은 권한 없음(Forbidden)을, 403은 무효한 요청 메서드(Request Method Not Allowed)를 나타낸다., 401은 서버 내부 오류, 403은 요청이 거절된 경우를 나타낸다., 401은 무효한 요청 메서드 사용을, 403은 무시당한 권한 문제를 나타낸다., 401은 인증되지 않은 상태에서 접근 시도를 나타내며, 403은 사용자에게 권한이 없는 리소스에 대한 요청을 나타낸다.], "correct": 3}
{"index": 36, "content": "JWT(JSON Web Token) 기반 인증에서 서버가 토큰의 유효성을 검증하는 방식으로 옳은 것은?", "options": [서버가 발급 시 사용한 비밀키(또는 개인키)로 토큰의 서명을 검증하고, 위조 여부를 확인한다., 서버가 매 요청마다 데이터베이스에 저장된 세션과 토큰을 대조하여 검증한다., 클라이언트가 스스로 서명을 검증한 뒤 유효 여부를 서버에 통보한다., 토큰에 포함된 만료 시간(exp)만 확인하고 서명은 검증하지 않는다.], "correct": 0}
{"index": 37, "content": "CORS 오류를 해결하기 위해 서버에서 설정해야 하는 것은 무엇인가요?", "options": [서버는 모든 출처에 대해 접근 권한을 주도록 설정하면 CORS 문제가 해결된다., 서버 측에 CORS 미들웨어를 설치하고, 원하는 출처에 대한 허용 목록을 설정한다., 클라이언트 측에서 요청 시 Access-Control-Allow-Origin 헤더를 추가하여 서버의 응답을 억제한다., 서버에서는 자동으로 CORS 문제를 해결하며, 개발자가 별도로 설정할 필요가 없다.], "correct": 1}
{"index": 38, "content": "다음 HTTP 메서드 중 멱등성(idempotency)이 보장되지 않는 것은?", "options": [PUT, DELETE, POST, GET], "correct": 2}
{"index": 39, "content": "대용량 파일 업로드를 처리할 때 서버 메모리 사용을 줄이는 방법은 무엇인가요?", "options": [파일 전송이 완료된 후에 한 번에 모든 내용을 메모리로 로드하여 처리한다., 대용량 파일 업로드는 서버의 대역폭만 고려하면 되며, 메모리는 크게 영향받지 않는다., 파일을 여러 작은 파트로 분해하여 순차적으로 업로드하면 메모리를 적게 사용할 수 있다., 스트림(Stream) 방식으로 파일을 읽어올 경우, 전체 데이터를 메모리에 올리지 않고 부분적으로 처리할 수 있어 메모리를 절약할 수 있다.], "correct": 3}
{"index": 40, "content": "웹 애플리케이션에서 SQLAlchemy 세션(Session)을 요청 단위로 관리하는 일반적인 패턴은?", "options": [요청이 시작될 때 세션을 새로 생성하고, 요청이 끝나면 커밋 또는 롤백한 뒤 세션을 닫는다., 세션은 스레드와 무관하게 항상 공유되며 별도로 종료할 필요가 없다., 세션은 첫 쿼리 실행 시점에만 생성되고 이후 재사용하지 않는다., 애플리케이션 시작 시 세션 하나를 생성해 전체 요청에서 재사용한다.], "correct": 0}
{"index": 41, "content": "커넥션 풀(connection pool) 크기를 실제 동시 요청 수보다 너무 작게 설정했을 때 발생하는 문제는?", "options": [쿼리 실행 속도가 항상 빨라진다., 동시 요청이 몰릴 때 커넥션을 얻지 못한 요청이 대기하거나 타임아웃 오류가 발생한다., 서버 메모리 사용량이 오히려 늘어난다., 데이터베이스가 자동으로 풀 크기를 늘려 대응한다.], "correct": 1}
{"index": 42, "content": "인덱스가 없는 컬럼으로 WHERE 조건을 거는 쿼리에서 발생할 수 있는 성능 문제는?", "options": [쿼리가 실행되지 않고 오류를 반환한다., 쿼리 옵티마이저가 자동으로 인덱스를 생성해 문제가 없다., 전체 테이블을 순차 스캔(full scan)하여 응답 시간이 늘어난다., 항상 캐시된 결과를 반환해 오히려 빨라진다.], "correct": 2}
{"index": 43, "content": "migrations 작업을 수행하기 위해 Alembic의 주요 명령어들은 무엇인가요?", "options": [migrations 작업에는 alembic upgrade 명령만 필요하며, 이는 데이터베이스 스키마를 최신 상태로 만든다., Alembic에서는 migrations 작업을 위해 단순히 python manage.py db init, python manage.py db migrate, python manage.py db upgrade와 같이 진행한다., Alembic에서는 migrations 작업을 위한 특정 명령어가 없으며, 직접 SQL 스크립트를 작성해야 한다., Alembic에서 migrations 작업은 alembic init 명령으로 시작하고, 이후 alembic revision 및 upgrade 명령으로 업데이트한다.], "correct": 3}
{"index": 44, "content": "Celery 워커(worker)와 브로커(broker, 예: Redis/RabbitMQ)의 역할은 무엇인가?", "options": [워커는 작업을 실행하고 결과를 반환하며, 브로커는 작업 요청(메시지)을 전달하는 큐 역할을 한다., 브로커는 작업을 실행하고 결과를 반환하며, 워커는 작업 요청을 큐에 추가한다., 워커와 브로커 모두 작업을 큐에 넣고 실행한다., 워커가 작업 요청을 처리하면서 동시에 큐 역할도 수행한다.], "correct": 0}
{"index": 45, "content": "Celery 태스크가 실패했을 때 자동으로 재시도되도록 설정하는 방법은?", "options": [재시도는 지원되지 않으며, 실패한 태스크는 수동으로 다시 호출해야 한다., @app.task(autoretry_for=(Exception,), max_retries=3)와 같이 데코레이터에 재시도 대상 예외와 최대 횟수를 지정한다., 태스크 함수 안에서 try/except로 감싸고 아무 것도 하지 않으면 Celery가 알아서 재시도한다., 브로커(Redis) 설정에서 retry=true 옵션을 켜면 모든 태스크가 자동 재시도된다.], "correct": 1}
{"index": 46, "content": "Celery 태스크가 재시도될 때 멱등성(idempotency)이 없으면 발생하는 문제는 무엇인가?", "options": [다른 태스크와 충돌하여 전체 작업이 실패할 위험이 생긴다., 재시도 과정에서 메모리 사용량이 폭증한다., 태스크가 중복 실행되어 결제·적립 등의 부작용이 두 번 이상 반영될 수 있다., 결과가 예측 가능하지 않아 시스템의 안정성이 저하된다.], "correct": 2}
{"index": 47, "content": "Celery 결과 백엔드(result backend)의 역할은 무엇인가?", "options": [결과 백엔드는 워커 프로세스를 관리한다., 결과 백엔드는 Celery 브로커와 통신하여 작업을 배포한다., 결과 백엔드는 작업 요청을 큐에 추가한다., 결과 백엔드는 태스크 실행 상태와 반환값을 저장하고 조회 가능하게 한다.], "correct": 3}
{"index": 48, "content": "Celery beat로 주기적 작업(periodic task)을 예약하는 표준적인 방법은?", "options": [beat_schedule 설정 또는 on_after_configure 시그널에서 sender.add_periodic_task()로 스케줄을 등록한다., @periodic_task 데코레이터만 사용하면 별도 설정 없이 자동으로 스케줄링된다., 워커 프로세스를 여러 개 띄우면 자동으로 주기적 실행이 활성화된다., 태스크 함수 안에 while True와 sleep()을 넣어 반복 실행한다.], "correct": 0}
{"index": 49, "content": "웹 요청 핸들러 안에서 처리하면 안 되고 Celery 태스크로 분리해야 하는 작업의 특징은 무엇인가?", "options": [작업이 사용자 인터페이스와 직결되는 경우., 작업이 오래 걸리거나 외부 서비스 호출처럼 응답 시간을 예측하기 어려운 경우, 작업이 단순하며 즉시 처리 가능한 경우, 작업이 요청과 직접적으로 연관된 경우], "correct": 1}
{"index": 50, "content": "캐시 무효화(cache invalidation) 전략 중 TTL(Time To Live)의 의미는?", "options": [TTL은 캐시가 사용할 수 있는 최대 메모리 용량을 의미한다., TTL은 캐시 키가 만료되는 시점을 지정하지만, 실제 삭제 작업이 수행되지 않는다., TTL은 캐시 키의 유효기간을 관리하며, 시간 제한이 경과되면 자동으로 캐시를 무효화한다., TTL은 캐시가 만료되기 전까지 사용될 수 있는 횟수를 지정한다.], "correct": 2}
{"index": 51, "content": "Redis를 이용한 cache-aside(캐시 조회 후 없으면 DB 조회 후 캐시 저장) 패턴 구현 순서는?", "options": [1. DB에서 데이터 가져오기, 2. Redis에서 해당 데이터의 캐시 키 조회, 3. 만약 없으면 Redis에 저장., 1. Redis에서 캐시 키 조회, 5초 동안 대기 후 다시 Redis 조회, 2. 없다면 DB에서 데이터 가져오기, 3. 가져온 데이터를 Redis에 저장., 1. 캐시 무효화 후 DB에서 데이터 가져오기, 2. Redis에 캐시 키가 있는지 확인, 3. 없다면 캐시 저장., 1. Redis에서 캐시 키 조회, 2. 없다면 DB에서 데이터 가져오기, 3. 가져온 데이터를 Redis에 저장.], "correct": 3}
{"index": 52, "content": "캐시 스탬피드(cache stampede)가 발생하는 조건과 이를 완화하는 방법으로 옳은 것은?", "options": [인기 키가 만료된 순간 다수의 요청이 동시에 캐시 미스를 겪어 DB로 몰리는 현상이며, 락(뮤텍스)이나 조기 재계산으로 완화한다., 캐시 서버가 다운되었을 때만 발생하며, 복제본을 추가하면 항상 방지된다., 캐시 키의 TTL이 충분히 남아있을 때 발생하며, 캐시 크기를 늘리면 해결된다., 여러 요청이 서로 다른 캐시 키를 조회할 때 발생하며, 키를 통합하면 해결된다.], "correct": 0}
{"index": 53, "content": "캐시 키(cache key)를 설계할 때 반드시 고려해야 할 사항은?", "options": [캐시 키에는 절대 콜론(:)이나 구분자를 사용하면 안 된다., 결과값에 영향을 주는 모든 파라미터(예: 사용자 ID, 필터 조건)를 키에 포함해 서로 다른 요청이 같은 키를 공유하지 않도록 한다., 키는 짧을수록 무조건 좋으므로 파라미터 없이 고정 문자열만 사용한다., 캐시 키는 대소문자를 구분하지 않으므로 신경 쓸 필요가 없다.], "correct": 1}
{"index": 54, "content": "pytest 픽스처(fixture)의 정의와 목적은 무엇인가?", "options": [픽스처는 코드 내의 반복되는 로직을 모듈화하고 재사용할 수 있게 하는 도구이다., 픽스처는 pytest에서 사용하는 특정 변수나 객체를 관리하는 설정 파일이다., 픽스처는 테스트 코드에서 공통적으로 사용되는 변수나 객체를 미리 준비해두는 함수이다., 픽스처는 프로젝트 설정 파일에 있는 모든 단위 테스트를 실행하기 위한 스크립트이다.], "correct": 2}
{"index": 55, "content": "pytest에서 외부 API 호출을 목(mock)으로 대체하는 방법은?", "options": [pytest에서 자체적으로 제공하는 @patch 메소드를 이용하면 된다., mock 모듈을 import하고, 함수 내에서 직접 API 호출 부분을 대체하도록 설정한다., 외부 API 호출 부분에 직접 mock객체로 교체한다., @mock.patch 데코레이터를 사용하여 필요한 위치에 테스트용 객체를 삽입한다.], "correct": 3}
{"index": 56, "content": "pip과 poetry의 의존성 관리 방식의 근본적인 차이는?", "options": [poetry는 poetry.lock으로 전체 의존성 트리를 고정해 재현 가능한 설치를 보장하지만, pip은 기본적으로 lock 파일 없이 requirements.txt만으로 설치한다., pip은 가상환경을 자동으로 생성하지만 poetry는 가상환경을 지원하지 않는다., poetry는 C로 작성되어 pip보다 설치 속도가 항상 빠르다., pip과 poetry는 동일한 파일 형식(pyproject.toml)을 사용해 차이가 없다.], "correct": 0}
{"index": 57, "content": "gunicorn 워커 프로세스 수를 `(2 × CPU 코어 수) + 1` 같은 공식으로 설정하는 이유는?", "options": [워커 수를 CPU 코어 수와 무관하게 최대한 늘리는 것이 항상 유리하기 때문이다., CPU 바운드 작업 기준으로 컨텍스트 스위칭 오버헤드를 줄이면서 CPU를 최대한 활용하기 위한 경험적 기준이기 때문이다., gunicorn은 코어 수보다 워커가 많으면 실행 자체가 거부되기 때문이다., 워커 수는 항상 요청 수와 정확히 일치해야 하기 때문이다.], "correct": 1}
{"index": 58, "content": "파이썬 가상환경(venv)을 만드는 표준 명령어는 무엇인가요?", "options": [pipenv shell, conda create --name myenv, python -m venv myenv, virtualenv myenv], "correct": 2}
{"index": 59, "content": "파이썬 패키지 구조에서 '__init__.py' 파일의 역할은 무엇인가요?", "options": [패키지 경로를 sys.path에 추가한다., 모듈 import 시 실행되도록 한다., 패키지 내부의 모든 모듈을 자동으로 가져온다., 파이썬 인터프리터에게 이 디렉토리를 패키지로 처리하도록 알려준다.], "correct": 3}
{"index": 60, "content": "pytest 커버리지(coverage) 측정 도구를 사용하여 테스트 커버리지를 확인하는 방법은 무엇인가요?", "options": [pytest --cov, coverage report, pytest --coverage, coverage run -m pytest], "correct": 0}
{"index": 61, "content": "uvicorn이 ASGI 애플리케이션을 실행하는 방식과, gunicorn과 함께 사용할 때의 설정은 무엇인가요?", "options": [ASGI는 WSGI와 같은 프레임워크로 uvicorn을 직접 실행하면 됩니다., uvicorn은 자체 이벤트 루프를 사용하여 ASGI 애플리케이션을 구동합니다. gunicorn에서는 -k 옵션으로 'uvicorn.workers.UvicornWorker'를 지정해야 합니다., ASGI는 uvicorn과 상호 호환되지 않습니다. 이를 위해 gunicorn은 별도의 브릿지 기능을 제공합니다., gunicorn에서 uvicorn 사용시, ASGI 애플리케이션을 직접 실행하지 않고 WSGI로 변환해야 합니다.], "correct": 1}
{"index": 62, "content": "환경변수로 민감한 설정값(DB 비밀번호 등)을 관리해야 하는 이유는 무엇인가요?", "options": [환경변수는 여러 개발자가 함께 작업할 때 서로 다른 설정 값을 관리하기 쉽습니다., 환경변수를 사용하면 설정 값이 런타임에 동적으로 변경될 수 있습니다., 환경 변수를 사용하면 설정 값이 소스 코드에 노출되지 않아 보안 위험이 줄어듭니다., 환경 변수는 테스트 환경에서만 작동하므로 개발과 프로덕션 사이의 차이나움을 방지합니다.], "correct": 2}
{"index": 63, "content": "파이썬 로깅에서 print() 대신 logging 모듈을 사용해야 하는 이유는 무엇인가요?", "options": [print() 함수를 사용하면 프로그램의 성능이 저하될 수 있습니다., 파이썬 로깅 시스템에서는 print() 대신 logging 모듈만을 지원합니다., logging 모듈은 출력할 메시지를 파일로만 기록할 수 있습니다., print() 함수보다 logging 모듈은 더 다양한 출력 포맷과 필터링 옵션을 제공합니다.], "correct": 3}
{"index": 64, "content": "정적 파일(static files)을 Gunicorn/Uvicorn 같은 애플리케이션 서버가 아니라 Nginx나 CDN으로 서빙해야 하는 주된 이유는?", "options": [애플리케이션 서버는 동적 요청 처리에 최적화되어 있어, 정적 파일을 대량으로 서빙하면 요청 처리 성능이 저하되기 때문이다., 정적 파일은 애플리케이션 서버에서 서빙하면 보안 취약점이 자동으로 생기기 때문이다., 정적 파일은 데이터베이스에 저장해야 하므로 별도 서버가 필요하기 때문이다., 정적 파일은 파이썬 코드로만 처리할 수 있기 때문이다.], "correct": 0}
{"index": 65, "content": "헬스체크(health check) 엔드포인트가 확인해야 할 항목은 무엇인가요?", "options": [애플리케이션 서버의 CPU 사용량만을 체크합니다., 데이터베이스 연결, 캐시 연결 등 핵심 의존 서비스의 가용성을 체크합니다., 네트워크 트래픽만을 모니터링 합니다., 애플리케이션이 실제로 동작하는지 여부를 확인하기 위한 단순한 테스트입니다.], "correct": 1}
{"index": 66, "content": "세션 기반 인증과 JWT 기반 인증 중 수평 확장(scale-out) 환경에 더 적합한 방식은 무엇이며 그 이유는 무엇인가요?", "options": [세션이나 JWT 모두 수평 확장 환경에서 동일하게 사용할 수 있습니다., 세션 인증이 수평 확장을 위해 설계되었으므로 세션 기반 인증을 선호합니다., JWT 인증은 토큰의 무상태성(statelessness)으로 인해 여러 서버 간에 별도 세션 저장소 없이 검증될 수 있으므로 JWT를 사용하는 것이 적합합니다., 세션이나 JWT 모두 단일 서버 환경에서만 유용하며 수평 확장에는 적합하지 않습니다.], "correct": 2}
{"index": 67, "content": "프로세스 기반(gunicorn worker) 병렬 처리와 스레드 기반 병렬 처리 중 GIL의 영향을 받는 것은 무엇인가요?", "options": [GIL은 별도의 프로세스 환경에서도 동일하게 적용됩니다., GIL은 모든 파이썬 프로그램에 동일하게 적용됩니다., gunicorn worker에서 실행되는 프로세스는 GIL의 제한을 피할 수 있습니다., 스레드 기반 병렬 처리는 GIL의 영향으로 인해 CPU 바운드 작업에서 성능 저하가 발생합니다.], "correct": 3}
{"index": 68, "content": "CPU 바운드 작업과 I/O 바운드 작업 중 각각 멀티프로세싱과 asyncio 중 어느 것이 더 적합한지 설명해주세요.", "options": [I/O 바운드 작업은 asyncio, CPU 바운드 작업은 멀티프로세싱이 더 적합합니다., 멀티프로세싱과 asyncio는 모든 유형의 작업에 동일하게 적용할 수 있습니다., CPU 바운드와 I/O 바운드 모두 스레딩만으로 충분히 처리할 수 있습니다., CPU 바운드 작업은 asyncio, I/O 바운드 작업은 멀티프로세싱이 더 적합합니다.], "correct": 0}
{"index": 69, "content": "여러 개발자가 각자 브랜치에서 동시에 Alembic 마이그레이션을 작성해 리비전 히스토리에 여러 head가 생겼을 때 해결 방법은?", "options": [가장 최근에 병합된 마이그레이션 파일만 남기고 나머지는 삭제한다., `alembic merge heads` 명령으로 여러 head를 하나의 병합 리비전으로 합친다., 각 개발자가 별도의 데이터베이스를 사용하면 head 충돌은 자동으로 해소된다., 마이그레이션 파일의 down_revision 값을 무시하고 최신 파일부터 순서대로 실행한다.], "correct": 1}
{"index": 70, "content": "다음 함수를 여러 번 호출할 때 어떤 문제가 발생하는가?\n\ndef add(item, items=[]):\n    items.append(item)\n    return items\n\nadd('a')\nadd('b')", "options": [함수를 호출할 때마다 items 매개변수가 새로운 빈 리스트로 초기화된다., add 함수는 매번 독립적인 items 리스트를 반환하므로 문제가 없다., items 매개변수의 기본값(list)은 함수 정의 시점에 단 한 번만 생성되어 호출 간에 공유·누적된다., append() 호출 후 items가 자동으로 새 리스트로 교체되므로 이전 값은 사라진다.], "correct": 2}
{"index": 71, "content": "다음 코드를 실행하면 nested_list는 어떻게 되는가?\n\nimport copy\nnested_list = [[1, 2], [3, 4]]\ncopied_list = copy.copy(nested_list)\ncopied_list[0].append(5)\nprint(nested_list)", "options": [append(5) 호출은 새 리스트를 반환할 뿐 기존 객체를 변경하지 않는다., copied_list와 nested_list는 완전히 독립된 메모리를 사용해 서로 영향을 주지 않는다., copy.copy()는 깊은 복사를 수행하므로 nested_list는 전혀 변경되지 않는다., copy.copy()는 최상위 리스트만 새로 만들고 내부의 중첩 리스트는 원본과 참조를 공유하므로, copied_list[0]을 변경하면 nested_list에도 반영된다.], "correct": 3}
{"index": 72, "content": "다음 코드의 동작으로 옳은 것은?\n\ndef generate_data():\n    for i in range(10):\n        yield i\n\ngen = generate_data()\nfirst_three = [next(gen) for _ in range(3)]\nrest = list(gen)", "options": [제너레이터는 멈춘 지점(yield 위치)의 상태를 유지하므로, next()로 3개를 먼저 소비한 뒤 남은 값을 순회하면 rest는 [3, 4, ..., 9]가 된다., 제너레이터를 한 번 next()로 소비하면 이후 list(gen)은 항상 빈 리스트를 반환한다., first_three와 rest에는 서로 겹치는 값이 포함된다., 제너레이터는 매번 처음부터 다시 시작하므로 rest도 [0, 1, ..., 9]가 된다.], "correct": 0}
{"index": 73, "content": "다음 코드에서 lambdas[0](1)의 결과는 무엇인가?\n\nnumbers = [1, 2, 3]\nlambdas = []\nfor number in numbers:\n    lambdas.append(lambda x: x + number)\nprint(lambdas[0](1))", "options": [for문이 끝나면 number 변수가 삭제되어 lambdas[0](1) 호출 시 NameError가 발생한다., 람다는 number를 지연 바인딩(late binding)하므로 모든 람다가 반복문 종료 시점의 마지막 number 값(3)을 참조해, lambdas[0](1)은 4를 반환한다., 리스트에 저장된 람다는 항상 numbers의 첫 번째 값(1)만 사용해 2를 반환한다., 각 람다는 생성 시점의 number 값을 즉시 복사해 저장하므로 서로 다른 값을 사용해 2를 반환한다.], "correct": 1}
{"index": 74, "content": "다음 데코레이터를 적용한 뒤 example.__name__을 출력하면 어떻게 되는가?\n\ndef simple_decorator(func):\n    def wrapper(*args, **kwargs):\n        return func(*args, **kwargs)\n    return wrapper\n\n@simple_decorator\ndef example():\n    \"\"\"This is an example function.\"\"\"\n    pass\n\nprint(example.__name__)", "options": [데코레이터가 example을 두 번 호출하게 만들어 부작용이 중복 발생한다., functools.wraps를 쓰지 않아도 __name__과 __doc__은 자동으로 보존되어 'example'이 출력된다., functools.wraps로 감싸지 않아 wrapper가 example을 대체하면서 __name__이 'wrapper'가 되고 __doc__도 사라진다., wrapper 함수가 example의 로직을 실행하지 않아 항상 None을 반환한다.], "correct": 2}
{"index": 75, "content": "다음 코드의 문제점은 무엇인가?\n\ndef risky_operation():\n    try:\n        result = 10 / 0\n        return result\n    except Exception:\n        pass", "options": [risky_operation 함수는 예외가 발생하면 자동으로 3회까지 재시도한다., pass문이 원래 예외를 새로운 예외로 감싸 상위 호출자에게 다시 던진다., except Exception: pass는 KeyboardInterrupt·SystemExit까지 잡아버려 프로그램을 강제 종료할 수 없게 만든다., 예외를 조용히 삼켜 실제 오류(ZeroDivisionError)가 발생해도 로그나 예외 정보가 전혀 남지 않아 디버깅이 어려워진다.], "correct": 3}
{"index": 76, "content": "다음 코드에서 GIL의 영향으로 실제로 벌어지는 일은?\n\nimport threading\n\ndef cpu_bound_task():\n    total = 0\n    for i in range(10_000_000):\n        total += i\n    return total\n\nthreads = [threading.Thread(target=cpu_bound_task) for _ in range(4)]\nfor t in threads:\n    t.start()\nfor t in threads:\n    t.join()", "options": [GIL로 인해 한 번에 하나의 스레드만 파이썬 바이트코드를 실행할 수 있어, CPU 바운드 순수 파이썬 루프는 스레드를 늘려도 속도가 거의 개선되지 않는다., 스레드 4개를 생성하면 CPU 코어 수와 무관하게 항상 4배의 속도 향상이 보장된다., GIL은 I/O 바운드 작업에도 동일하게 적용되어 스레드 사용이 전혀 무의미하다., threading.Thread는 CPython에서 별도 프로세스로 실행되어 GIL의 제한을 받지 않는다.], "correct": 0}
{"index": 77, "content": "다음 코드를 실행하면 어떤 일이 벌어지는가?\n\nclass CustomError(object):\n    pass\n\ntry:\n    raise CustomError('Custom error')\nexcept Exception as e:\n    print(e)", "options": [CustomError가 Exception을 상속하지 않아 except Exception 절을 통과해 잡히지 않고 프로그램이 그대로 종료된다., raise CustomError(...) 시점에 파이썬이 'exceptions must derive from BaseException' TypeError를 발생시키고, 이 TypeError가 except Exception에 잡혀 출력된다., CustomError('Custom error')가 정상적으로 발생하고 except Exception이 이를 잡아 'Custom error'를 출력한다., object를 상속한 클래스도 자동으로 BaseException의 서브클래스로 취급되어 정상 동작한다.], "correct": 1}
{"index": 78, "content": "path 파일의 내용이 숫자가 아닐 때 이 코드를 실행하면 어떤 문제가 발생하는가?\n\ndef read_config(path):\n    f = open(path, 'r')\n    data = f.read()\n    value = int(data)\n    f.close()\n    return value", "options": [ValueError가 발생하면 파이썬이 자동으로 f.close()를 호출한 뒤 예외를 전파한다., 숫자가 아닌 내용을 읽으면 read()가 예외를 발생시켜 파일이 아예 열리지 않는다., int(data)에서 ValueError가 발생해 f.close()가 실행되지 않고 파일 핸들이 닫히지 않은 채로 남는다., open()이 이미 파일을 자동으로 닫으므로 close() 호출 여부는 결과에 영향을 주지 않는다.], "correct": 2}
{"index": 79, "content": "다음 코드를 실행하면 어떤 일이 벌어지는가?\n\ndef get_user_age(age: int) -> int:\n    return age * 2\n\nresult = get_user_age('25')", "options": [타입 힌트가 자동으로 문자열을 정수로 변환해 result는 50이 된다., mypy 같은 정적 타입 검사기가 없어도 파이썬 인터프리터가 실행 시점에 타입을 검증해 오류를 낸다., 타입 힌트가 int로 지정되어 있으므로 문자열 '25'를 전달하면 파이썬이 자동으로 TypeError를 발생시킨다., 타입 힌트는 런타임에 강제되지 않으므로 문자열 '25'가 그대로 전달되고, age * 2는 문자열을 반복해 '2525'를 반환한다.], "correct": 3}
{"index": 80, "content": "다음 Django 뷰에서 어떤 문제가 발생할까요?\n\nfrom django.http import HttpResponse\nfrom .models import Order\n\ndef order_list(request):\n    orders = Order.objects.all()\n    for order in orders:\n        print(order.customer.name)\n    return HttpResponse('OK')", "options": [N+1 쿼리가 발생합니다., 주문 리스트가 정상적으로 출력됩니다., customer의 이름이 None으로 출력됩니다., view 함수가 예외를 던집니다.], "correct": 0}
{"index": 81, "content": "다음 코드에서 마지막 Item.objects.create(sku='B1') 줄에서 어떤 일이 벌어지는가?\n\nfrom django.db import transaction, IntegrityError\nfrom .models import Item\n\ndef create_items():\n    with transaction.atomic():\n        Item.objects.create(sku='A1')\n        try:\n            Item.objects.create(sku='A1')  # sku는 unique 제약\n        except IntegrityError:\n            pass\n        Item.objects.create(sku='B1')\n    return 'done'", "options": [IntegrityError가 atomic 블록 안에서 잡혔으므로 B1 생성은 정상적으로 수행된다., atomic 블록 내부에서 DB 수준 예외가 발생하면 해당 트랜잭션이 rollback 대상으로 표시되어, 이후 같은 블록 안의 쿼리 실행 시 TransactionManagementError가 발생한다., A1이 두 번 생성되어 데이터베이스에 중복 레코드가 남는다., IntegrityError는 자동으로 무시되며 이후 코드는 아무 영향을 받지 않는다.], "correct": 1}
{"index": 82, "content": "다음 FastAPI 엔드포인트에서는 어떤 문제가 발생할까요?\n\nfrom fastapi import APIRouter\nimport time\n\nrouter = APIRouter()\n\n@router.get('/slow')\nasync def read_slow():\n    time.sleep(5)\n    return {'message': 'done'}", "options": [time.sleep(5)는 5초 뒤 자동으로 취소되어 응답이 즉시 반환된다., time.sleep()이 코루틴이 아니므로 서버가 즉시 TypeError를 던지며 요청이 실패한다., time.sleep()은 동기(블로킹) 호출이라 5초 동안 이벤트 루프 전체가 멈춰, 같은 워커가 처리 중인 다른 요청들도 함께 지연된다., async def로 선언했으므로 FastAPI가 time.sleep()을 자동으로 별도 스레드에서 실행해준다.], "correct": 2}
{"index": 83, "content": "다음 DRF 코드에서 카테고리 목록을 조회하면 어떤 문제가 발생할까요?\n\nfrom rest_framework import serializers, viewsets\nfrom .models import Category, Product\n\nclass ProductSerializer(serializers.ModelSerializer):\n    class Meta:\n        model = Product\n        fields = ['id', 'name']\n\nclass CategorySerializer(serializers.ModelSerializer):\n    products = ProductSerializer(many=True, read_only=True)\n    class Meta:\n        model = Category\n        fields = ['id', 'name', 'products']\n\nclass CategoryViewSet(viewsets.ModelViewSet):\n    queryset = Category.objects.all()\n    serializer_class = CategorySerializer", "options": [쿼리셋이 정상적으로 조회됩니다., 카테고리와 제품들이 정상적으로 매핑됩니다., 뷰세트에서 예외가 발생합니다., N+1 쿼리가 발생합니다.], "correct": 3}
{"index": 84, "content": "다음 Django 시그널 리시버에서는 어떤 문제가 발생할까요?\n\nfrom django.db.models import signals\nfrom .models import Product\n\ndef product_saved(sender, instance, created, **kwargs):\n    if not created:\n        instance.last_synced = True\n        instance.save()\n\nsignals.post_save.connect(product_saved, sender=Product)", "options": [instance.save()가 post_save 시그널을 다시 트리거해 무한 재귀 호출되고 결국 RecursionError로 이어질 수 있다., 업데이트된 제품만 한 번 처리되고 정상적으로 종료된다., 제품 저장이 정상적으로 한 번만 수행된다., 뷰 함수에서 예외가 발생한다.], "correct": 0}
{"index": 85, "content": "이 태스크가 네트워크 오류로 실패해 Celery가 자동으로 재시도하면 어떤 문제가 발생할 수 있는가?\n\nfrom celery import shared_task\nfrom .models import User\n\n@shared_task(bind=True, max_retries=3)\ndef add_credit(self, user_id, amount):\n    user = User.objects.get(id=user_id)\n    user.balance += amount\n    user.save()", "options": [balance 필드는 자동으로 초기화되므로 재시도해도 금액이 누적되지 않는다., user.balance += amount 연산은 실행할 때마다 잔액을 더하므로, 재시도로 같은 태스크가 두 번 실행되면 금액이 중복 반영된다., Celery는 같은 인자로 재시도되는 태스크를 자동으로 감지해 중복 실행을 막아준다., 재시도 시 user_id가 자동으로 바뀌어 다른 사용자에게 적립된다.], "correct": 1}
{"index": 86, "content": "다음 SQLAlchemy 세션 관리 코드에서는 어떤 문제가 발생할까요?\n\nfrom sqlalchemy.orm import sessionmaker\n\nSession = sessionmaker(bind=engine)\n\ndef get_session():\n    db_session = Session()\n    return db_session", "options": [커넥션 수가 제한됩니다., 예외가 발생합니다., 세션이 누적됩니다., 쿼리셋이 정상적으로 조회됩니다.], "correct": 2}
{"index": 87, "content": "다음 FastAPI 의존성 주입에서는 어떤 동작이 일어날까요?\n\nfrom fastapi import APIRouter, Depends\n\ncall_count = 0\n\nasync def get_user_data():\n    global call_count\n    call_count += 1\n    return {'count': call_count}\n\nrouter = APIRouter()\n\n@router.get('/users')\nasync def read_users(a=Depends(get_user_data), b=Depends(get_user_data)):\n    return {'a': a, 'b': b}", "options": [call_count 값이 요청마다 자동으로 0으로 초기화된다., 두 개의 Depends를 동시에 쓰면 FastAPI가 예외를 발생시킨다., get_user_data가 매 요청마다, 그리고 같은 요청 안에서도 매번 새로 호출된다., 같은 요청 안에서 get_user_data가 두 번 쓰였지만 FastAPI가 결과를 캐시해 실제로는 한 번만 호출되므로 a와 b는 같은 값을 갖는다.], "correct": 3}
{"index": 88, "content": "다음 pytest 픽스처 코드에서는 어떤 문제가 발생할까요?\n\nimport pytest\n\nclass TestOrders:\n    @pytest.fixture(scope='session')\n    def cart(self):\n        items = []\n        yield items\n\n    def test_add_item(self, cart):\n        cart.append('apple')\n        assert cart == ['apple']\n\n    def test_cart_starts_empty(self, cart):\n        assert cart == []", "options": [픽스처가 session 스코프이므로 테스트 간에 cart 리스트 상태가 공유되어, 실행 순서에 따라 test_cart_starts_empty가 실패할 수 있다., pytest가 각 테스트마다 자동으로 새 cart를 생성해 두 테스트 모두 항상 통과한다., session 스코프는 클래스 안에서는 자동으로 function 스코프로 취급된다., 픽스처가 제너레이터(yield)이므로 매 테스트마다 새로 생성된다.], "correct": 0}
{"index": 89, "content": "다음 Redis 캐시 코드에서는 어떤 문제가 발생할까요?\n\nimport redis\n\ncache = redis.Redis()\n\ndef get_value(key):\n    value = cache.get(key)\n    if not value:\n        result = db.query(key)\n        cache.set(key, result, ex=60)\n        return result\n    return value", "options": [락을 사용하지 않아도 Redis가 내부적으로 요청을 자동 직렬화해 문제가 없다., 캐시 키가 만료된 순간 동시에 몰린 다수의 요청이 전부 캐시 미스를 겪어 db.query가 동시에 여러 번 호출되는 캐시 스탬피드가 발생할 수 있다., ex=60 옵션이 없으므로 캐시가 영구히 만료되지 않는다., cache.get()이 항상 예외를 던져 db.query가 절대 호출되지 않는다.], "correct": 1}
{"index": 90, "content": "다음 인증 함수를 실행하면 만료된 토큰에 대해 어떤 결과가 나오는가?\n\nimport jwt\n\nSECRET = 'my-secret'\n\ndef authenticate(token):\n    try:\n        payload = jwt.decode(token, SECRET, algorithms=['HS256'], options={'verify_exp': False})\n        return True\n    except jwt.InvalidSignatureError:\n        return False", "options": [서명이 유효하지 않으면 만료 여부와 관계없이 항상 인증에 실패한다., options 파라미터는 서명 알고리즘을 지정하는 용도이며 만료 검증과는 무관하다., options={'verify_exp': False}로 인해 만료 시간 검증이 꺼져 있어, 서명만 유효하면 만료된 토큰도 인증에 통과한다., PyJWT는 항상 만료 시간을 강제로 검증하므로 이 옵션은 아무 효과가 없다.], "correct": 2}
{"index": 91, "content": "다음 SQLAlchemy 엔진 설정에서 동시 요청이 10개 몰릴 때 어떤 문제가 발생할 수 있는가?\n\nfrom sqlalchemy import create_engine\n\nengine = create_engine('postgresql://user:pass@localhost/dbname', pool_size=5, max_overflow=0)\n\ndef fetch_data():\n    conn = engine.connect()\n    try:\n        result = conn.execute('SELECT * FROM users')\n        return result.fetchall()\n    finally:\n        conn.close()", "options": [max_overflow=0은 무제한 오버플로우를 허용한다는 의미이므로 문제가 없다., pool_size는 요청 수와 무관하게 항상 자동으로 늘어난다., conn.close()가 finally에 있으므로 커넥션 풀 부족 문제는 발생하지 않는다., pool_size=5, max_overflow=0으로 최대 5개 커넥션만 허용되어, 6번째 이상 요청은 커넥션을 기다리다 타임아웃될 수 있다.], "correct": 3}
{"index": 92, "content": "두 마이그레이션 파일이 각각 아래와 같이 작성되었다. 이 상태에서 `alembic upgrade head`를 실행하면 어떤 문제가 발생하는가?\n\n# migration_a.py\nrevision = 'a1b2'\ndown_revision = 'base_rev'\n\n# migration_b.py\nrevision = 'c3d4'\ndown_revision = 'base_rev'", "options": [두 마이그레이션이 같은 down_revision('base_rev')을 가리켜 리비전 히스토리에 head가 두 개 생기고, Alembic이 어느 것을 최신으로 적용해야 할지 알 수 없다는 오류를 낸다., Alembic이 자동으로 두 마이그레이션을 시간순으로 정렬해 순서대로 적용한다., down_revision이 같아도 revision 값이 다르므로 아무 문제 없이 순차 적용된다., 먼저 작성된 파일이 자동으로 무시되고 나중 파일만 적용된다.], "correct": 0}
{"index": 93, "content": "다음 코드에서 might_fail() 태스크가 예외를 던지면 results 변수에는 무엇이 담기는가?\n\nimport asyncio\n\nasync def might_fail():\n    raise ValueError('boom')\n\nasync def succeed():\n    return 'ok'\n\nasync def main():\n    results = await asyncio.gather(might_fail(), succeed(), return_exceptions=True)\n    print(results)", "options": [return_exceptions=True는 예외를 무시하고 완전히 삭제하므로 results에는 'ok' 하나만 담긴다., return_exceptions=True이므로 gather()는 예외를 던지지 않고, results에 [ValueError('boom'), 'ok']처럼 예외 객체와 정상 결과가 함께 담긴다., return_exceptions=True와 관계없이 첫 예외가 발생하면 gather()가 즉시 예외를 던져 results에는 아무 값도 담기지 않는다., 예외가 발생한 태스크는 자동으로 재시도되어 results에는 두 개의 정상 결과만 담긴다.], "correct": 1}
{"index": 94, "content": "다음 코드를 실행할 때 사용자가 username에 `' OR '1'='1`을 입력하면 어떤 문제가 발생하는가?\n\nuser_input = input('Username: ')\n\ndef fetch_user():\n    query = f\"SELECT * FROM users WHERE username='{user_input}'\"\n    result = execute_query(query)\n    return result", "options": [execute_query가 내부적으로 파라미터 바인딩을 수행해 안전하게 처리된다., 작은따옴표가 포함된 입력은 자동으로 거부되어 쿼리가 실행되지 않는다., 입력값이 SQL 문자열에 그대로 삽입되어, 조건절이 항상 참이 되며 전체 사용자 정보가 조회되는 SQL 인젝션이 발생한다., f-string은 특수문자를 자동으로 이스케이프하므로 인젝션이 발생하지 않는다.], "correct": 2}
{"index": 95, "content": "다음 Django settings.py를 운영 서버에 그대로 배포했을 때 발생할 수 있는 보안 문제는?\n\nDEBUG = True\nALLOWED_HOSTS = []\n\ndef custom_404(request, exception):\n    return render(request, '404.html', status=404)", "options": [DEBUG는 로컬 개발 편의를 위한 옵션일 뿐 운영 환경 보안과는 무관하다., ALLOWED_HOSTS가 비어 있으면 DEBUG 설정과 무관하게 모든 요청이 차단되어 안전하다., custom_404 핸들러가 정의되어 있으므로 DEBUG 값과 무관하게 항상 404.html만 노출된다., DEBUG=True 상태에서 처리되지 않은 예외가 발생하면, 소스 코드 경로·설정값·스택트레이스 등 민감한 정보가 담긴 디버그 페이지가 그대로 클라이언트에 노출된다.], "correct": 3}
{"index": 96, "content": "다음 코드를 실행하면 어떤 일이 벌어지는가?\n\ndict_data = {1: 'A', 2: 'B'}\n\ndef modify_dict():\n    for key in dict_data:\n        if dict_data[key] == 'B':\n            del dict_data[key]\n    return dict_data", "options": [딕셔너리를 순회하는 도중 항목을 삭제해 RuntimeError: dictionary changed size during iteration이 발생한다., 삭제된 항목은 건너뛰고 나머지 항목에 대해 정상적으로 순회가 끝난다., del은 다음 반복이 끝난 뒤에 실제로 적용되므로 예외 없이 실행된다., 딕셔너리 키를 순회할 때는 항목을 삭제해도 항상 안전하다.], "correct": 0}
{"index": 97, "content": "다음 코드를 실행한 뒤 original 리스트의 값은 어떻게 되는가?\n\ndef sort_list(user_list):\n    user_list.sort()\n    return user_list\n\noriginal = [3, 1, 2]\nsorted_list = sort_list(original)", "options": [정렬된 결과만 반환되므로 원본 리스트에는 영향이 없다., sort()는 리스트를 제자리(in-place)에서 정렬하므로 original도 [1, 2, 3]으로 바뀐다., 리스트는 함수 호출 후에도 원래 상태([3, 1, 2])를 유지한다., sort()는 정렬된 새 리스트를 반환할 뿐 원본은 바뀌지 않는다.], "correct": 1}
{"index": 98, "content": "다음 엔드포인트를 호출했을 때, some_background_task 내부에서 예외가 발생하면 클라이언트는 어떤 응답을 받는가?\n\nfrom fastapi import APIRouter, BackgroundTasks\n\ndef some_background_task():\n    raise ValueError('background failure')\n\nrouter = APIRouter()\n\n@router.post('/endpoint')\ndef endpoint(background_tasks: BackgroundTasks):\n    background_tasks.add_task(some_background_task)\n    return {'status': 'Task started'}", "options": [add_task에 등록된 함수는 즉시 동기적으로 실행되므로 예외가 나면 엔드포인트 자체가 실패한다., 예외가 발생하면 FastAPI가 자동으로 재시도해 성공할 때까지 응답을 지연시킨다., 백그라운드 작업은 응답이 클라이언트로 전송된 이후에 실행되므로, 예외가 발생해도 클라이언트는 이미 200과 {'status': 'Task started'}를 받은 뒤다., 백그라운드 작업의 예외는 응답 전송 전에 처리되어 클라이언트가 500 오류를 받는다.], "correct": 2}
{"index": 99, "content": "다음 함수에서 필수 환경변수 `KEY`가 설정되어 있지 않을 때 어떤 일이 벌어지는가?\n\nimport os\n\ndef get_setting():\n    key = os.environ.get('KEY')\n    connect_to_service(key)", "options": [os.environ.get은 os.environ[...]과 동일하게 키가 없으면 KeyError를 발생시킨다., 환경변수가 없으면 파이썬이 자동으로 빈 문자열을 채워 넣어 오류 없이 동작한다., get() 메서드는 시스템 전체 환경변수를 다시 로드해 누락된 값을 찾아낸다., os.environ.get('KEY')는 키가 없으면 예외 없이 None을 반환해, connect_to_service(None)이 그대로 호출되며 설정 누락이 조용히 넘어간다.], "correct": 3}
